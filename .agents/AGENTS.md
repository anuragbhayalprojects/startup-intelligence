# Startup Intelligence Workspace Rules

## Running servers
- Whenever you are asked to run the frontend and backend servers (e.g., via `./run.sh`), you must also proceed with Ollama server enablement to ensure the local AI model (e.g., `qwen2.5:3b`) is running and available.
